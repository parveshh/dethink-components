# Registry

This folder contains shadcn-compatible registry metadata and generated payloads.

Current status:

- `items/base.json` validates the initial base payload shape.
- Button registry metadata will be added after the Button PRD and local issues are approved.

